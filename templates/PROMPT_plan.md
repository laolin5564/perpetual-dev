0a. Study `specs/*` to learn the application specifications.
0b. Study IMPLEMENTATION_PLAN.md (if present; it may be incorrect).
0c. For reference, the application source code is in `src/*`.

1. Study existing source code and compare against `specs/*`. Prioritize tasks and
   create/update IMPLEMENTATION_PLAN.md as a bullet point list sorted by priority.
   Search for TODOs, minimal implementations, placeholders, skipped tests,
   and inconsistent patterns. Confirm with code search before assuming anything
   is missing.

IMPORTANT: Plan only. Do NOT implement anything.

GOAL: We want to achieve [PROJECT-SPECIFIC GOAL]. If an element is missing,
confirm it doesn't exist, then author the spec at specs/FILENAME.md and add
the implementation plan to IMPLEMENTATION_PLAN.md.
